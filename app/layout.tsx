import type {Metadata} from 'next';import './globals.css';const title='Física Resolvida — Questões e provas';const description='Pratique física com questões comentadas, vídeos de resolução e gere suas próprias listas de atividades.';export const metadata:Metadata={title,description,openGraph:{title,description,images:['/og.png']},twitter:{card:'summary_large_image',title,description,images:['/og.png']}};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="pt-BR"><body>{children}</body></html>}
